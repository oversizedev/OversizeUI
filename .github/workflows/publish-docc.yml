<<<<<<< HEAD
name: Deploy DocC
on:
  push:
    branches: ["develop"]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: macos-12
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      - name: Build DocC
        run: |
          xcodebuild docbuild -scheme OversizeUI \
            -derivedDataPath /tmp/docbuild \
            -destination 'generic/platform=iOS';
          $(xcrun --find docc) process-archive \
            transform-for-static-hosting /tmp/docbuild/Build/Products/Debug-iphoneos/OversizeUI.doccarchive \
            --hosting-base-path OversizeUI \
            --output-path docs;
          echo "<script>window.location.href += \"/documentation/oversizeui\"</script>" > docs/index.html;
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: 'docs'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
=======
name: Publish DocC

<<<<<<< HEAD
      - name: Deploy to Github Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docs
>>>>>>> 07be3de (Create publish-docc.yml)
=======
on:
  push:
    branches: ["main"]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v3
    - name: Generate DocC
      run: |
        mkdir -p docs
        set -o pipefail && xcodebuild docbuild \
          -scheme OversizeUI \
          -destination "generic/platform=iOS" \
          OTHER_DOCC_FLAGS="--transform-for-static-hosting --hosting-base-path --output-path docs" \
          | xcpretty
    - name: Deploy to Github Pages
      uses: JamesIves/github-pages-deploy-action@v4.4.1
      with:
        branch: docc
        folder: docs
        target-folder: docs
        git-config-name: Mobile Automation
        git-config-email: alexander@oversize.design
        commit-message: Updated DocC documentation for version
>>>>>>> 3aa832d (Update publish-docc.yml)
