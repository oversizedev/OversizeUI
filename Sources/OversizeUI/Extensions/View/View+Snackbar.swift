//
// Copyright © 2021 Alexander Romanov
// Created on 26.08.2021
//

/*
 import SwiftUI

 public extension View {
    func embedInSnackbar(text: String, showSnackbar: Bool) -> some View {
        self
            .overlay(
                VStack {
                    Spacer()

                    Snackbar {
                        Text(text)
                    }
                    .offset(y: showSnackbar ? -100 : 100)
                    .animation(.spring())
                }
            )
    }
 }
 */
